# Laptop development environment (Windows/Linux/macOS).
# Jetson Nano (Ubuntu 18.04, JetPack 4.x, Python 3.6) needs its OWN pinned
# set - these wheels do not exist for that platform. See docs/DEPLOYMENT.md.

torch>=2.1
numpy>=1.24
opencv-python>=4.8
mediapipe>=0.10.9
pyyaml>=6.0
pytest>=7.4
tqdm>=4.65
