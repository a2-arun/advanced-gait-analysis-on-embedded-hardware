# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
_scan_result.txt

# Model weights - not committed. The GaitGraph2 weights carry no license, so
# each machine converts its own copy (docs/SETUP.md). The center .npy is ours.
models/checkpoint/*.pth
models/checkpoint/*.onnx
models/checkpoint/*.engine
models/pose_model/*.task

# Runtime data - regenerated locally, never shared across machines
database/*.db
outputs/logs/*.log
outputs/events/*.json
# Pose landmarks of the 13 research subjects (biometric data) - never commit
outputs/research_poses.pkl

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
